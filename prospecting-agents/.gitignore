# PRIVACY: dati reali e master NON entrano MAI nel repo (spec § Regole trasversali #1)
*.xlsx
!evals/**/*.example.*
master_*.xlsx
export/
archivio/

# Output di lavoro (contengono PII dei lead)
backup/*
logs/*
reports/*
bozze/*
!backup/.gitkeep
!logs/.gitkeep
!reports/.gitkeep
!bozze/.gitkeep

# Config con path reali: versionato solo il template config.example.yaml.
# config.yaml è locale a ogni macchina (non tracciato) → niente conflitti a ogni pull.
config.yaml
config.local.yaml

# Eval con dati reali (solo l'esempio è versionato)
evals/emails_labeled.csv
evals/replies_labeled.jsonl

# Python
__pycache__/
*.pyc
.pytest_cache/
.venv/
venv/
*.egg-info/
